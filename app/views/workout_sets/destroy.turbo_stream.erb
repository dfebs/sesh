<%= turbo_stream.remove dom_id(@workout_set) %>
<%= turbo_stream.replace dom_id(@workout_session, :complete) do %>
  <%= render "workout_sessions/complete_workout_session", workout_session: @workout_set.workout_session %>
<% end %>
