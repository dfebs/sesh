<h1><%= workout_instance.workout.name %></h1>

<%= link_to "Add Set", new_workout_session_workout_instance_workout_set_path(workout_session, workout_instance), data: { turbo_frame: combined_dom_id(workout_session, workout_instance, WorkoutSet.new) } %>

<div id="<%= dom_id(workout_instance, :sets) %>">
  <%= render "workout_sets/workout_sets", workout_instance: workout_instance %>
</div>

<%= turbo_frame_tag combined_dom_id(workout_session, workout_instance, WorkoutSet.new) %>
