<%= link_to workout.name, workout_path(workout)%>
<%= render "tags/tags", tags: workout.tags %>
<p><%= workout.description %></p>
<p>Highest Volume: <%= workout.highest_volume %> <%= workout.unit %></p>
<%= link_to "Edit", edit_workout_path(workout) %>
